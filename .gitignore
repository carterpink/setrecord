node_modules
dist
out

# Bundled Recall language model — ~1.9 GB, fetched at build time by
# scripts/fetch-model.mjs. Too large to commit; ships via electron-builder.
resources/models/
*.gguf
.DS_Store
.eslintcache
*.log*

# Environment / secrets
.env
.env.*
!.env.example

# License signing — ISSUER ONLY, never commit. The private key is the money key.
scripts/mint-license.mjs
*.pem
*-key.pem
license-*.pem
LICENSE_PRIVATE_KEY*
license-keys/
*.license

# Claude Code temporary worktrees (not part of the app)
.claude/worktrees/
