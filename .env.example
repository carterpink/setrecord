# Crash reporting (opt-in, main process only)
# Set this to your Sentry DSN to enable crash report collection.
# Leave blank to disable reporting entirely (default for local/CI builds).
# Users must also enable crash reporting in Settings for reports to be sent.
# Never commit a real DSN here — add it to your CI environment secrets.
SENTRY_DSN=
