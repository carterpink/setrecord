# Sentry error tracking (opt-in, main + renderer)
# Set to your Sentry DSN to enable error tracking. Users must also opt-in in Settings.
# Leave blank to disable error tracking entirely (default for local/CI builds).
# Never commit a real DSN here — add it to your CI environment secrets.
VITE_SENTRY_DSN=
